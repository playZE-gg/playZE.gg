---
interface Props {
  /** Stage number, e.g. 1 */
  n: number;
  /** Stage heading text, e.g. "Grassy Section" */
  title: string;
}
const { n, title } = Astro.props;
const padded = String(n).padStart(2, '0');
---
<h3 class="stage-title"><span class="stage-badge">{padded}</span>{title}</h3>
